<%= form_for(@atmosphere) do |f| %>
  <% if @atmosphere.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@atmosphere.errors.count, "error") %> prohibited this atmosphere from being saved:</h2>

      <ul>
      <% @atmosphere.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
