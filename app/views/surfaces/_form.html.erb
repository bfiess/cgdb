<%= form_for(@surface) do |f| %>
  <% if @surface.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@surface.errors.count, "error") %> prohibited this surface from being saved:</h2>

      <ul>
      <% @surface.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
